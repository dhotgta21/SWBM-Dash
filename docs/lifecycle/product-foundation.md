# Product Foundation - Demo Builder Merchant

- **Status:** DRAFT for plan (brownfield; shell already exists)
- **Updated:** 2026-08-04

## Workflows (presenter)

1. **Prep:** Set demo env, seed DB, set vertical pack for prospect.
2. **Marketing walk:** Homepage (vertical story) → catalogue → product → quote CTA.
3. **Ops walk:** Admin login → dashboard KPIs → clients → client detail → invoices → open invoice PDF.
4. **Reset:** Wipe + reseed before next meeting.

## Navigation (existing; no IA redesign)

- Public: `/`, `/catalogue`, `/products`, `/quote`, content pages
- Staff: `/dashboard`, `/clients`, `/invoices`, `/settings/*`
- Auth: `/admin-login`, `/login`

## Features on the map

| Feature | Surface change |
|---------|----------------|
| Demo brand | Labels, titles, company name |
| Dense history | Data only (UI already exists) |
| Vertical packs | Landing copy + category emphasis |
| Sample SKUs | Products + category meta |

## Visual system

- Keep existing appearance tokens / Brand settings.
- Demo does not require a new design system.
- Optional later: DEMO banner (F-demo-banner).

## Inspiration

- **NONE** (reuse live Star Hawk product UI as the design; rebrand only).
- Logged: no inspiration images requested; invent-not-applicable.
